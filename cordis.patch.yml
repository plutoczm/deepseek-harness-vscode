# VPN/stability bridge only.
# The SSH workspace provider must be installed explicitly as a top-level
# Harness plugin, so this patch must not insert dsh-ssh-remote again.
- insert:
    - id: dsh-ssh-vpn-bridge
      name: dsh-ssh-vpn-bridge
