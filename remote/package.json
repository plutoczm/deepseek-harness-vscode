{
  "name": "deepseek-harness-remote",
  "version": "0.4.0",
  "private": true,
  "description": "Desktop launcher for the official DeepSeek Harness on SSH hosts with per-session Python environments, optional local proxy fallback, and event-driven API cost display.",
  "main": "desktop/main.cjs",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "start:no-open": "node src/server.mjs --no-open",
    "desktop": "electron desktop/main.cjs",
    "desktop:dir": "electron-builder --dir",
    "desktop:win": "electron-builder --win nsis --x64",
    "check": "node --check src/config.mjs && node --check src/ssh.mjs && node --check src/files.mjs && node --check src/network.mjs && node --check src/usage-cost.mjs && node --check src/manager.mjs && node --check src/server.mjs && node --check public/app.js && node --check public/workspace-picker.js && node --check public/ui-polish.js && node --check public/network-options.js && node --check desktop/main.cjs && node --check harness-plugin/index.js && node --check harness-plugin/usage-cost.js && bash -n harness-plugin/bash-env.sh && bash test/bash-env.test.sh",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "electron": "^43.2.0",
    "electron-builder": "^26.15.7"
  },
  "build": {
    "appId": "io.github.plutoczm.deepseek-harness-remote",
    "productName": "DeepSeek Harness Remote",
    "asar": false,
    "directories": {
      "buildResources": "build",
      "output": "dist"
    },
    "files": [
      "desktop/**/*",
      "src/**/*",
      "public/**/*",
      "harness-plugin/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ],
      "icon": "build/icon.svg",
      "artifactName": "DeepSeek-Harness-Remote-${version}-Setup-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowElevation": true,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "DeepSeek Harness Remote"
    }
  }
}
