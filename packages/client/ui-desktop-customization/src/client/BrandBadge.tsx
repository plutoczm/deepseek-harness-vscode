/** Branding badge intentionally disabled in this distribution. */

import type { ReactNode } from 'react'

/** Keep source attribution in LICENSE/README, but render no shell badge. */
export function BrandBadge(): ReactNode {
  return null
}
